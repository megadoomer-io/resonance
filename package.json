{
  "name": "resonance-ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Front-end unit tests for the Resonance lineup builder.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
